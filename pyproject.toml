[project]
name = "license-plate-recognition"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hermeschen1116", email = "108386417+hermeschen1116@users.noreply.github.com" } 
]
requires-python = ">=3.12"
dependencies = [
    "torch>=2.4.1",
    "transformers>=4.45.2",
]

[project.scripts]
license-plate-recognition = "license_plate_recognition:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "catppuccin-jupyterlab>=0.2.4",
    "datasets>=3.0.1",
    "huggingface-hub>=0.25.2",
    "jupyter>=1.1.1",
    "python-dotenv>=1.0.1",
    "roboflow>=1.1.48",
    "ultralytics>=8.3.15",
]
